server:
  port: 8082

spring:
  application:
    name: product-service
  datasource:
    url: jdbc:mysql://${AIVEN_MYSQL_HOST:localhost}:${AIVEN_MYSQL_PORT:3306}/${PRODUCT_DB_NAME:ecommerce_product_db}?useSSL=${AIVEN_MYSQL_SSL:true}&requireSSL=${AIVEN_MYSQL_SSL:true}&serverTimezone=UTC
    username: ${AIVEN_MYSQL_USER:root}
    password: ${AIVEN_MYSQL_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

app:
  jwt:
    secret: ${JWT_SECRET}
  seed:
    enabled: ${SEED_DATA_ENABLED:true}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

info:
  app:
    name: Product Service
